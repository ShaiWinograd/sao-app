{
  "name": "spaceorder-attendance-scheduler",
  "version": "0.0.1",
  "private": true,
  "description": "Azure Timer-triggered Function that drives the attendance sweep (spec §16.2/§16.4/§17.3). Deployed independently from the API so it can be enabled/disabled/redeployed without touching the API.",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.2"
  }
}
